// This interface will store the user details of current loggedin user that will come
// from backend server

export interface UserDetails {
    username: String;
    password: String;
    role: String;
}